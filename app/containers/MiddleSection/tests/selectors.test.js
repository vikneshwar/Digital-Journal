// import { selectMiddleSectionDomain } from '../selectors';

describe('selectMiddleSectionDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
